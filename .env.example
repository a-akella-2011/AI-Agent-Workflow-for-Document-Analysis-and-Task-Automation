# Copy to .env and fill values. Do NOT commit real credentials.
# For local Llama2 mode (no API key needed):
AGENT_MODE=local
LLAMA_MODEL_PATH=/path/to/ggml-model.bin
DOCUMENTS_DIR=documents
SAFE_TOOL_CONFIRMATION=interactive
EMBED_MODEL_NAME=all-MiniLM-L6-v2

# If you want to use OpenAI instead (costs may apply):
# AGENT_MODE=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
